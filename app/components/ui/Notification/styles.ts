import {StyleSheet} from 'react-native';
import {UI_GREEN_500} from '../../../styles';

export default StyleSheet.create({
  container: {
    backgroundColor: UI_GREEN_500,
    padding: 12,
    paddingVertical: 8,
    borderRadius: 12,
  },
  label: {
    color: '#FFF',
    textAlign: 'center',
  },
});
