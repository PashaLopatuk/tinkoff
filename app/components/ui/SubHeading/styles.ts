import {StyleSheet} from 'react-native';
import {UI_GREY_800} from '../../../styles';

export default StyleSheet.create({
  text: {
    fontSize: 20,
    fontWeight: 'bold',
    color: UI_GREY_800,
  },
  textCenter: {
    textAlign: 'center',
  },
});
