import {StyleSheet} from 'react-native';
import {UI_GREY_100} from '../../../styles';

export default StyleSheet.create({
  root: {
    width: '100%',
    marginTop: 12,
    padding: 12,
    borderRadius: 12,
    backgroundColor: 'white',
  },
});
