import {StyleSheet} from 'react-native';
import {UI_GREY_800} from '../../../styles';

export default StyleSheet.create({
  text: {
    fontSize: 24,
    fontWeight: 'bold',
    color: UI_GREY_800,
    marginBottom: 16,
  },
  textCenter: {
    textAlign: 'center',
  },
});
